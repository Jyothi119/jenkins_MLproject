pipeline {
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                script {
                    docker.image('python').inside('-u root -v C:/ProgramData/Jenkins/.jenkins/workspace/kidney_disease ML model') {
                        sh 'pip install -r requirements.txt'
                    }
                }
            }
        }
        stage('Train') {
            steps {
                script {
                    docker.image('python').inside('-u root') {
                        sh 'python trainingML.py'
                    }
                }
            }
        }  
    }
}
