pipeline {
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                script {
                    docker.image('continuumio/anaconda3').inside('-u root -v C:/ProgramData/Jenkins/.jenkins/workspace/kidneydisease -w /kidney_app') {
                        sh 'pip install -r requirements.txt'
                    }
                }
            }
        }
        stage('Train') {
            steps {
                script {
                    docker.image('continuumio/anaconda3').inside('-u root -v C:/ProgramData/Jenkins/.jenkins/workspace/kidneydisease -w /kidney_app') {
                        sh 'python trainingML.ipynb'
                    }
                }
            }
        }  
    }
}
