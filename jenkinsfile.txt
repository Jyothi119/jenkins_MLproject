pipeline {
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                script {
                    docker.image('continuumio/anaconda3').inside('C:\\ProgramData\\Jenkins\\.jenkins\\workspace\\kidneydisease') {
                        sh 'pip install -r requirements.txt'
                    }
                }
            }
        }
        stage('Train') {
            steps {
                script {
                    docker.image('continuumio/anaconda3').inside('C:\\ProgramData\\Jenkins\\.jenkins\\workspace\\kidneydisease') {
                        sh 'python trainingML.ipynb'
                    }
                }
            }
        }  
    }
}
