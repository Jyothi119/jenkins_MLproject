pipeline {
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                script {
                    docker.image('python:latest').inside('-u root -v C:/ProgramData/Jenkins/.jenkins/workspace/kidney_disease ML model:/kidney_app') {
                        sh 'pip install -r requirements.txt'
                    }
                }
            }
        }
        stage('Train') {
            steps {
                script {
                    docker.image('python:latest').inside('-u root -v C:/ProgramData/Jenkins/.jenkins/workspace/kidney_disease ML model:/kidney_app') {
                        sh 'python trainingML.py'
                    }
                }
            }
        }  
    }
}
